def choice(graph, node, current_candidate):
    # Call metropolis sampler and store in variable 

    # Means we made the switch
    if node.color == current_candidate:
        pass

    
    # If it rejects it then flip the direction
    else:
        node.direction *= -1